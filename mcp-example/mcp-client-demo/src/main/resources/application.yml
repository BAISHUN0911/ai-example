server:
  port: 10003

spring:
  application:
    name: mcp-client-demo

  ai:
    zhipuai:
      api-key: d4a15a67bd9c4694bb77ce6a1d4f842c.TfydnFOMQDKj6zcx
      base-url: "https://open.bigmodel.cn/api/paas"
      chat:
        options:
          model: glm-4-air-250414
    mcp:
      client:
        enabled: true
        name: mcp-demo-client
        version: 1.0.0
        type: SYNC  # or ASYNC for reactive applications
        request-timeout: 15s
#        stdio:
#          servers-configuration: classpath:/mcp-servers-config.json
        sse:
          connections:
            idaas-v2:
              url: http://localhost:8080/idaas-api-v2
  main:
    web-application-type: servlet


# 调试日志
logging:
  level:
    io:
      modelcontextprotocol:
        client: DEBUG
        spec: DEBUG
